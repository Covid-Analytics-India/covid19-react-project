export const globalStyles = (theme) => ({
  '@global': {
    body: {
      backgroundColor: theme.palette.background.default,
    },
    html: {
      height: '100%',
      backgroundColor: theme.palette.background.default,
    },
  },
});
